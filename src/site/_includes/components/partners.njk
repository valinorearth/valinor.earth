<ul class="logo-list">
  {% for p in partners %}
  <li>
    <a class="tpp-logo" title="{{p.name}}" href="{{p.url}}" target="_blank">
      <span
        class="w-full h-full aspect-ratio aspect-ratio-contain aspect-ratio-4/3"
      >
        <img width="120" src="{{p.logoPath | url}}" alt="Logo of {{p.name}}" />
      </span>
    </a>
  </li>
  {% endfor %}
</ul>
